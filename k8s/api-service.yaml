apiVersion: v1
kind: Service
metadata:
  name: bookapi
  namespace: default
spec:
  selector:
    app: bookapi
  type: LoadBalancer
  ports:
  - name: bookapi
    protocol: TCP
    port: 8080
    targetPort: 8080
